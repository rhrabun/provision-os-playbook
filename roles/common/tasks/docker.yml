- name: Create Docker group
  become: yes
  group:
    name: docker
    state: present

- name: Add user to Docker group
  become: yes
  user:
    name: "{{ ansible_user }}"
    groups:
      - docker
    append: yes
