packages_to_install:
  # GUI
  - tilix
  # Python-related
  - python3-pip
  # General utilities
  - curl
  - wget
  - htop
  - git
  - unzip
  - bash-completion
  - openssh-server
  - dconf-cli
  - vim
  # Docker
  - apt-transport-https
  - ca-certificates
  - gnupg-agent
  - software-properties-common

pymodules_to_install:
  - virtualenv
  - awscli
  - httpie
  - ansible
  - tldr
